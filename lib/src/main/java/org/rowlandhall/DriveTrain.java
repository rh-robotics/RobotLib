package org.rowlandhall;

/**
 * The DriveTrain interface represents the drive train subsystem of the robot. It extends the Subsystem interface, which means that it must implement the operatorUpdate method, allowing it to update its state based on the operator control inputs during the teleop phase of the OpMode.
 */
public interface DriveTrain extends Subsystem {
}
